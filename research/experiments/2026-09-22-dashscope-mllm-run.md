# DashScope MLLM execution attempt

- Date: 2026-09-22
- Scope: OSI scene 0000, hardened six-condition Phase-B package
- Protocol: 60 answer-blind requests generated by `prepare_mllm_evaluation.py`
- Intended model: `qwen3-vl-32b-instruct`
- Intended decoding: temperature 0, maximum 64 output tokens
- Compatible endpoint: `https://maas.qianwenaiapi.com/compatible-mode/v1`

## Implementation

`reconstruction/run_dashscope_mllm_evaluation.py` sends only the images and prompts already declared by each prepared request. It stores the requested/returned model names, response ID, usage, latency, answer, and raw API response. Results are written atomically after every request and completed request IDs are skipped on restart. Transient transport errors, rate limits, and server failures are retried with bounded exponential backoff.

The local request-construction test verifies that images are encoded as data URLs, temperature remains zero, and no `ground_truth` field is added. The complete MLLM test module passes four tests in WSL.

## Attempted run and observed blocker

The first real image request did not obtain an API response. One Python attempt ended during TLS with `SSLEOFError`; a later direct connectivity check reached the Bailian host and received HTTP/2 404 for a HEAD request, establishing basic DNS/TLS reachability. The next clean login-shell inspection found `DASHSCOPE_API_KEY` length zero. The `.bashrc` contains one export directive, but its persisted value is empty.

No prediction was recorded, no score was computed, and no downstream result is claimed. The next run must first restore the credential privately, confirm presence without printing it, execute exactly one request, inspect its response and usage, and only then resume the remaining requests.
